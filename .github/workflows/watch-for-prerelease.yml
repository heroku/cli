name: Watch for pre release

on:
  push:
    branches:
      - prerelease/*

jobs:
  start-pre-release:
    uses: ./.github/workflows/create-cli-release.yml
    secrets: inherit
    with:
      isProdTrigger: ${{ false }}
