#!/usr/bin/env bash

source ./scripts/circle/_init

set -x

echo "Releasing ${CHANNEL}"

ls tmp/darwin_x64
ls tmp/linux_x64

# release to s3
"${root}/scripts/circle/_release_tarballs"

if [[ "${OS}" = "darwin" ]]; then
  "${root}/scripts/darwin/homebrew"
fi
