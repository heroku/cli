machine:
  node:
    version: v6.2.0
dependencies:
  pre:
    - heroku update
    - sudo add-apt-repository ppa:duggan/bats -y
    - sudo apt-get update
    - sudo apt-get install bats
    - npm install
    - heroku plugins:link .
